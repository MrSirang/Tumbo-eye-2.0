# Frontend API (optional — defaults to /api via Vite proxy in dev)
VITE_API_URL=/api

# Google OAuth Web Client ID (same as backend Google:ClientId)
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID.apps.googleusercontent.com
